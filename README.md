# Gesture-Controlled-Car
Gesture Controlled Car is an Arduino-based project where an MPU6050 detects hand tilt and sends F/B/L/R/S motion commands via nRF24L01. The receiver Arduino reads those wireless commands and controls a motor driver to move the car forward, backward, left, right, or stop in real time.
